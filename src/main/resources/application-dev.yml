server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/project?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone = GMT
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
#nacos 配置
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      discovery:
        username: nacos
        password: nacos
      config:
        file-extension: yaml

  application:
    name: demo-server
  profiles:
    active: dev


# mysql日志打印
logging:
  level:
    com.huida.ybs.mapper: DEBUG
    org.springframework: ERROR

